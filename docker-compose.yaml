version: '3.3'

services:

    mysql:
        
        name: mysql
        image: mysql:5.7
        
        volumes:
          
          - mysql-data:/var/lib/mysql
        
        restart: always
        
        environment:
        
            MYSQL_ROOT_PASSWORD: mysql
            MYSQL_DATABASE:      test
            MYSQL_USER:          mysql
            MYSQL_PASSWORD:      mysql

volumes:

    mysql-data: